export class ServerEvents {
    static readonly PROMPT_MESSAGE = "prompt_message";
}

export class ClientEvents {
    static readonly RECEIVE_PROMPT_MESSAGE = "receive_prompt_message";
    static readonly MAX_RETRY = 3;
}